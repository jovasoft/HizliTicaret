@model List<DiscountViewModel>
@{

    ViewData["Title"] = "Index";
    Layout = "../Shared/Layouts/_AdminLayout.cshtml";
}

<!-- Layout container -->
<div class="layout-container">
    <!-- Layout content -->
    <div class="layout-content">

        <!-- Content -->
        <div class="container-fluid flex-grow-1 container-p-y">

            <h4 class="d-flex justify-content-between align-items-center w-100 font-weight-bold py-3 mb-4">
                <div>İndirimler</div>

                <a href="/Admin/AddDiscount"><button type="button" class="btn btn-primary btn-round d-block"><span class="ion ion-md-add"></span>&nbsp; İndirim Ekle</button></a>
            </h4>


            <div class="card">
                <div class="card-datatable table-responsive">
                    <table id="discount-list" class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th>Kategori/Ürün</th>
                                <th>İndirim Oranı</th>
                            </tr>
                        </thead>
                        <tbody>
                            @*@foreach (var discount in Model)
                            {
                                <tr id="@discount.CategoryId">
                                    <td>

                                        
                                        @{
                                            if (category.CategoryType == 0)
                                            {
                                                <span>Kadın</span>
                                            }
                                            if (Convert.ToInt32(category.CategoryType) == 1)
                                            {
                                                <span>Erkek</span>
                                            }
                                            if (Convert.ToInt32(category.CategoryType) == 2)
                                            {
                                                <span>Çocuk</span>
                                            }
                                        }
                                    </td>
                                    <td>
                                        <label class="btn btn-outline-primary btn-sm float-right removeDis">Sil</label>
                                    </td>

                                </tr>
                            }*@
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
        <!-- / Content -->
    </div>
    <!-- Layout content -->

</div>
<!-- / Layout container -->

@*<script type="text/javascript">

    $('.removeDis').on('click', function () {
        var categoryId = $(this).parent().parent().attr("id");

        $.ajax({
            url: '/Admin/DeleteCategory',
            type: 'POST',
            data: { id: categoryId }
        }).done(function () { $('#' + categoryId).remove(); });

    });
</script>*@
